% !TEX root = ../main.tex
\chapter[Trazabilidad de las corridas]{Trazabilidad de las Corridas en el Almacén de Experimentos}
\label{anexo:mlflow}

La Sección~\ref{sec:experiment_tracking} describe cómo cada ejecución del sistema, sea del modo
que sea, abre una corrida de MLflow y escribe sus artefactos dentro del directorio que esa
corrida tiene asignado. Este apéndice muestra el resultado tal como lo ve quien abre el almacén,
porque es la evidencia de que las cifras que la memoria publica proceden de ejecuciones
registradas y no de un cuaderno de trabajo.

Las capturas se han tomado sobre el almacén del propio repositorio (\texttt{mlflow.db} y
\texttt{mlruns/}), navegable con \texttt{make mlflow-ui}. Se recorta en todas ellas la columna de
contenido, dejando fuera el menú lateral de la herramienta. La Figura~\ref{fig:mlflow_run_overview}
muestra la ficha de una corrida y la Figura~\ref{fig:mlflow_run_artifacts} sus artefactos, ambas
sobre la corrida que respalda el Capítulo~\ref{chap:resultados}; la
Figura~\ref{fig:mlflow_tuning_convergence} recoge la convergencia de una búsqueda de
hiperparámetros, que es el otro uso que el sistema hace del almacén.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/mlflow/mlflow_run_overview.png}
    \caption{Ficha de la corrida \texttt{tfg\_latent\_20260831\_113339}, que es la rama de demanda
    reconstruida del experimento a 500 series y la fuente de la Tabla~\ref{tab:metrics_predictive}.
    Lo relevante para la reproducibilidad son las \textbf{etiquetas}: \texttt{git\_commit} fija el
    código que produjo el resultado, \texttt{config\_hash} la configuración resuelta con la que se
    ejecutó, y \texttt{series}, \texttt{panel\_rows} y \texttt{supervised\_rows} la escala a la que
    se midió, que es el campo sin el cual dos corridas no son comparables (Anexo~\ref{anx:tuning_imputador}).
    Debajo, las 72 métricas de la corrida indexadas por modelo y estrategia de datos: las tres
    primeras visibles son el MAE y el RMSE de CatBoost sobre demanda latente, exactamente los
    valores que recoge el Capítulo~\ref{chap:resultados}.}
    \label{fig:mlflow_run_overview}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/mlflow/mlflow_run_artifacts.png}
    \caption{Artefactos de esa misma corrida. Conviven los dos resúmenes que el
    Capítulo~\ref{chap:resultados} explota por separado (\texttt{metrics\_summary.csv} para la
    calidad del pronóstico y \texttt{cost\_summary.csv} para la de la decisión), los dos ficheros
    de predicciones que se computan sobre conjuntos de filas distintos
    (\texttt{validation\_predictions.csv} y \texttt{predictions.csv}), y el resto de salidas que la
    Sección~\ref{sec:experiment_tracking} enumera: el desglose por \textit{fold}, la decisión de
    promoción, el plan de reposición, las excepciones marcadas y los informes de calidad y de
    deriva. Con estos ficheros cualquier métrica publicada es recomputable sin volver a entrenar.}
    \label{fig:mlflow_run_artifacts}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/mlflow/mlflow_tuning_convergence.png}
    \caption{Convergencia de la búsqueda de hiperparámetros del modelo de \textit{forecasting}
    (corrida \texttt{forecasting\_lightgbm\_v4}, 60 \textit{trials}), registrada métrica a métrica
    con el número de \textit{trial} como paso. A la izquierda el coste de cada \textit{trial}, que
    recorre un rango de 22 a 30 sin tendencia aparente: la superficie de respuesta es ruidosa. A la
    derecha el mejor coste acumulado, que cae una sola vez en torno al \textit{trial} 20 y se
    mantiene plano durante los cuarenta restantes. La lectura conjunta es la que justifica el
    protocolo del Anexo~\ref{anx:tuning_imputador}: el valor objetivo de un \textit{trial} suelto
    dice poco, porque la dispersión entre ellos domina la diferencia entre configuraciones.}
    \label{fig:mlflow_tuning_convergence}
\end{figure}
