repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.4.1
    hooks:
      # Linter
      - id: ruff
        args: [ --fix ]
      # Formatter
      - id: ruff-format
